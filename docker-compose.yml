version: '3.2'

services:

  redis:
    image: redis
    expose:
     - 6379
    networks:
     - mediasort

  app:
    image: dasos/mediasort
    ports:
     - 8000:8080
    networks:
     - mediasort
    #volumes:
     #- /input:/input
     #- /output:/output
     #- /delete:/delete
    environment:
     - 'FLASK_REDIS_URL=redis://redis:6379/0'
     #- 'FLASK_DRY_RUN=FALSE'
     #- 'FLASK_INPUT_DIR=/input'
     #- 'FLASK_SUMMARY_ITEMS=/input'

networks:
 mediasort:
